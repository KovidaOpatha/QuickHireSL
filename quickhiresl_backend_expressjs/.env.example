# QuickHireSL Backend Environment Configuration

# Server Configuration
# The port on which the Express.js server will run
PORT=3000

# Database Configuration
# MongoDB connection string - replace with your own connection string
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.<id>.mongodb.net/<database>?retryWrites=true&w=majority

# Authentication
# Secret key for JWT token generation and verification
# Make sure to use a strong, unique secret in production
JWT_SECRET=your_secret_key_here

# File Storage
# Optional: Configure file upload paths if your app supports file uploads
# UPLOAD_PATH=./uploads

# CORS Configuration
# Optional: Configure allowed origins for CORS
# ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com
